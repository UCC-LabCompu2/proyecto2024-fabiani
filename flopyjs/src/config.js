const CONFIG = {
    screen: { width: 400, height: 400},
    inputMap: {
        up: ['ArrowUp', 'w'],
        down: ['ArrowDown', 's'],
        left: ['ArrowLeft', 'a'],
        right: ['ArrowRight', 'd']
    }
}

export default CONFIG;
